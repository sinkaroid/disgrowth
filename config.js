module.exports = {
  botId: "724047481561809007", // Your bot's ID
  memCacheExpire: 30, // How long to keep the data in memory cache (in minutes)
  alertOnFetch: false // Alert when API is called, then memCache was expired
};